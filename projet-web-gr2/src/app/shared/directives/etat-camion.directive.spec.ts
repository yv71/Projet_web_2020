import { EtatCamionDirective } from './etat-camion.directive';

describe('EtatCamionDirective', () => {
  it('should create an instance', () => {
    const directive = new EtatCamionDirective();
    expect(directive).toBeTruthy();
  });
});
