import { EtatStructureDirective } from './etat-structure.directive';

describe('EtatStructureDirective', () => {
  it('should create an instance', () => {
    const directive = new EtatStructureDirective();
    expect(directive).toBeTruthy();
  });
});
