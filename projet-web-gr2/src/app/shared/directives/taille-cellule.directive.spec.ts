import { TailleCelluleDirective } from './taille-cellule.directive';

describe('TailleCelluleDirective', () => {
  it('should create an instance', () => {
    const directive = new TailleCelluleDirective();
    expect(directive).toBeTruthy();
  });
});
